{
  "compilerOptions": {
    "target": "ES2020",         // Biên dịch sang cú pháp JS hiện đại
    "module": "CommonJS",      // Dùng module kiểu Node.js
    "outDir": "./dist",        // Thư mục chứa code JS sau khi biên dịch
    "rootDir": "./src",        // Thư mục chứa code TS gốc
    "strict": true,            // Bật chế độ nghiêm ngặt (rất tốt)
    "esModuleInterop": true,   // Cho phép 'import' các package 'require'
    "skipLibCheck": true       // Bỏ qua kiểm tra file .d.ts
  },
  "include": ["src/**/*"] // Chỉ biên dịch các file trong thư mục 'src'
}